{
  "name": "hmi-middleware",
  "version": "0.1.0",
  "description": "HMI middleware — reads input device, transforms signals, broadcasts over WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@serialport/parser-readline": "^13.0.0",
    "node-hid": "^3.1.0",
    "serialport": "^13.0.0",
    "ws": "^8.18.0"
  }
}
